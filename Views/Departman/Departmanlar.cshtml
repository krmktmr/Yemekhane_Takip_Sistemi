@{
    Layout = "~/Views/Shared/_LayoutDenetim.cshtml";
}

@using YemekHaneTakipOtomasyonu.Models
@model List<kod_departman>
<link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
<link href="~/css/Index.css" rel="stylesheet" />
<div class="container index">
    <h2>Departmanlar</h2>
    <div class="table-wrapper">
        <table class="fl-table">
            <thead>
                <tr>
                    <th>id</th>
                    <th>Departman Adi</th>
                    <th>Düzenle</th>
                    <th>Sil</th>
                </tr>
            </thead>
            <tbody>

                @{
                    foreach (var x in Model)
                    {

                        <tr>
                            <td>@x.id</td>
                            <td>@x.aciklama</td>
                            <td><a href="/Departman/DepartmanDuzenle/@x.id" class="btn btn-success">Düzenle</a></td>
                            <td><a href="/Departman/DepartmanSil/@x.id" class="btn btn-danger">Sil</a></td>
                        </tr>
                    }
                    <tr>
                        <td colspan="4"><a href="/Departman/DepartmanEkle" class="btn btn-info">Departman Ekle</a></td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
